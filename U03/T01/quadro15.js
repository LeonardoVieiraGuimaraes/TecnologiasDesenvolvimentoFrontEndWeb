// JavaScript complexo como filhos JSX
// Quadro 15 – JavaScript complexo
const frutas = ["Maçã", "Banana", "Cereja"];
const elemento = (
  <ul>
    {frutas.map((frutas) => (
      <li>{fruta}</li>
    ))}
  </ul>
);